import { jersey } from '@/utils/helper'
import React from 'react'

function BlogButton() {
  return (
    <>
     <button className={` button-54 ${jersey.className}`}>Search</button> 
    </>
  )
}

export default BlogButton
